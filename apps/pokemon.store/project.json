{
  "name": "pokemon.store",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/pokemon.store",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/pokemon.store"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/pokemon.store"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "pokemon.store:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "pokemon.store:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "pokemon.store:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nx/next:export",
      "options": {
        "buildTarget": "pokemon.store:build:production"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/pokemon.store/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/pokemon.store/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
